# Deep sleep after 1h idle; any keypress wakes it (~1s reconnect).
# Halves only — never enable sleep on the dongle: its mock kscan cannot wake it.
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000
